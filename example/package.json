{
  "name": "typed-translator-example",
  "version": "0.0.1",
  "description": "Example of typed-translator",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./dist",
    "typed-translator": "typed-translator ./resources ./types/typed-translator.d.ts",
    "start": "npm run clean && npm run typed-translator && tsc && node ./dist/src/index.js"
  },
  "author": "mmktomato",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "typed-translator": "file:.."
  }
}
